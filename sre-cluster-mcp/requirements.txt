# ============================================================
# sre-cluster-mcp — Python MCP Server
# ============================================================
# Core MCP framework
fastmcp>=0.9.0

# HTTP client for calling Spring Boot Actuator + cache APIs
httpx>=0.27.0
httpx[asyncio]

# HTTP server bridge
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Docker SDK
docker>=7.1.0

# Prometheus HTTP API client
prometheus-api-client>=0.5.4

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support
anyio>=4.0.0

# Logging + structured output
structlog>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
