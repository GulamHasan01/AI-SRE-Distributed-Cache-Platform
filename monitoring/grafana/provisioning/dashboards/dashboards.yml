apiVersion: 1

providers:
  - name: 'Distributed Cache Platform'
    orgId: 1
    folder: 'Cluster Overview'
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
