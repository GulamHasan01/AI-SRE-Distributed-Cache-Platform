# ============================================================
# sre-agent — Python SRE Agent + FastAPI Bridge
# ============================================================

# LLM + Agent framework
anthropic>=0.34.0
langgraph>=0.2.0
langchain-anthropic>=0.2.0
langchain-core>=0.3.0

# FastAPI HTTP bridge
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# MCP client (to call sre-cluster-mcp over HTTP/SSE)
httpx>=0.27.0
httpx-sse>=0.4.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async
anyio>=4.0.0

# Structured logging
structlog>=24.0.0

# Markdown / postmortem generation
jinja2>=3.1.0

# Prometheus client for self-monitoring
prometheus-client>=0.21.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
